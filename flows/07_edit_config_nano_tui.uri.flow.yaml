flow:
  id: 07-edit-config-nano-tui
  description: Edit config file in nano (TUI).

defaults:
  approved: true
  dry_run: false

do:
  - shell://sh:
      args: ["-c", "echo '# urisys lab' > /tmp/urisys-lab.conf"]
  - him://local/keyboard/command/hotkey:
      keys: ["ctrl", "alt", "t"]
  - him://local/keyboard/command/type-text:
      text: "nano /tmp/urisys-lab.conf"
  - him://local/keyboard/command/key:
      key: Return
  - him://local/keyboard/command/hotkey:
      keys: ["ctrl", "x"]
  - him://local/keyboard/command/key:
      key: n
  - kvm://local/monitor/primary/query/screenshot

# Effect-level contract asserted by scripts/run_test_sessions.py (uri2flow ignores it).
# Opening nano in a terminal must visibly change the desktop.
expect:
  screen_changed: true
